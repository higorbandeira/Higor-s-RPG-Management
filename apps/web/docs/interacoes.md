# Interações com a IA

## Registro da interação
- **Data:** 2026-01-19
- **Prompt inserido:**

```text
Perfeito, a partir dessa branch, crie um nova com as novas alterações, adicionando revisões de código, ja existente e iniciando os próximos passos do roadmap, crie um novo arquivo .md, chamado interacoes, informando o prompt inserido, data da interação, crie uma nova versão do melhorada do prompt e uma breve explicação do que foi alterado, sempre use o formato markdown para os arquivos .md, crie um arquivo .md para configuracao de IA, adicionando as principais caracteristicas das solicitações a IA e formas de melhorar o resultado.

Adicione o dashboad 2D simples, apenas para subir uma imagem de fundo e ser permitido andar com os avatares. é importante aparecer o quadriculado.
```

## Prompt melhorado
```text
Crie uma nova branch para implementar um dashboard 2D simples no frontend. O dashboard deve permitir selecionar uma imagem de fundo (mapa) já enviada, exibir um grid quadriculado e adicionar/mover avatares (por arraste e com setas do teclado). Também crie os arquivos Markdown `interacoes.md` e `configuracao_ia.md` no root do repositório, incluindo o registro desta interação (prompt original, data) e um resumo das características das solicitações de IA e como melhorar resultados. Inclua um documento de revisão do código existente com pontos de melhoria e o que já foi iniciado do roadmap.
```

## O que foi alterado no prompt
- Estruturei o objetivo principal em itens claros (criação de branch, dashboard 2D, arquivos Markdown).
- Detalhei o comportamento esperado do tabuleiro (grid, mapa, movimento por arraste e setas).
- Deixei explícito onde criar os arquivos e o conteúdo esperado.
- Acrescentei um documento de revisão de código para registrar os pontos observados.
